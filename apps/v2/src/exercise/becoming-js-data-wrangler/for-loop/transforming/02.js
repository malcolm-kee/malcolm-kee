/**
 * Returns an array of string that describe each
 * person in dataset in the format of '{name}, aged {age}'.
 * If `age` is not available, returns '{name}' only.
 *
 * An example of the dataset is
 *
 * ```js
 * const dataset = [
 *   {
 *     name: 'Malcolm',
 *     age: 32,
 *   },
 *   {
 *     name: 'Esther',
 *     age: 10,
 *   },
 *   {
 *     name: 'Richie',
 *     age: 50,
 *   },
 *   {
 *     name: 'Audrey',
 *   },
 * ];
 * ```
 *
 * When the example is provided to the function,
 * it should returns:
 * ```js
 * ['Malcolm, aged 32', 'Esther, aged 10', 'Richie, aged 50', 'Audrey']
 * ```
 */
export default function getPersonsSummary(dataset) {
  // TODO
}
