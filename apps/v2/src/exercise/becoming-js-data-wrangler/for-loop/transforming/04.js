/**
 * Given an array of strings representing integers, returns
 * a new array where each element is an object with two properties,
 * the original integer string and the integer value squared.
 *
 * Example:
 *
 * Given `numberStrings` is ['1', '2', '3', '4', '5'],
 * the function should returns:
 *
 * ```js
 * [
 *   {
 *     original: '1',
 *     squared: 1,
 *   },
 *   {
 *     original: '2',
 *     squared: 4,
 *   },
 *   {
 *     original: '3',
 *     squared: 9,
 *   },
 *   {
 *     original: '4',
 *     squared: 16,
 *   },
 *   {
 *     original: '5',
 *     squared: 25,
 *   },
 * ]
 * ```
 *
 */
export default function processIntegers(numberStrings) {
  // TODO
}
