---
title: Install TypeScript
description: 'Learn type alias, interface, and index signature.'
date: '2019-10-19'
updated_at: '2019-12-07'
---

## Introducing TypeScript in Create React App project

```bash
yarn add -D typescript @types/react @types/react-dom @types/node @types/jest @types/classnames
```

OR

```bash
npm i -D typescript @types/react @types/react-dom @types/node @types/jest @types/classnames
```

<aside>

What are those `@types/??` packages? They are the third-party libraries that are maintained by communities. We will discuss them in more details in a [coming section](/typescript-for-react-developer/using-third-party-types#using-types-from-typescript-and-libraries).

</aside>

Note: For brevity, going forwared I will only show the yarn version. Just change `yarn add` to `npm i` for equivalent command for npm.

## Convert JavaScript to TypeScript

- id
- copy
- typecheck v1

<aside>

[Commit](https://github.com/malcolm-kee/react-ecomm-site/commit/7da1014a393c6a457b340cb648b324d0053dfbd7)

</aside>

## Configure Styleguidist to Parse TypeScript

```bash
yarn add -D react-docgen-typescript
```

```js fileName=styleguide.config.js
const path = require('path'); // highlight-line

module.exports = {
  components: 'src/components/**/*.{jsx,tsx}',
  // highlight-start
  propsParser: (filePath, source, resolver, handlers) => {
    const { ext } = path.parse(filePath);
    return ext === '.tsx'
      ? require('react-docgen-typescript').parse(
          filePath,
          source,
          resolver,
          handlers
        )
      : require('react-docgen').parse(source, resolver, handlers);
  },
  // highlight-end
  ...
};
```

<aside>

[Commit](https://github.com/malcolm-kee/react-ecomm-site/commit/4fe8924eefa32d34cbae50701470e7398f77e933)

</aside>

## Convert Simple React Component to TypeScript

<!-- just change file extension -->
- `<Footer />`: rename `footer.jsx` to `footer.tsx`

## Convert Function to TypeScript

- Rename `copy.js` to `copy.ts`.