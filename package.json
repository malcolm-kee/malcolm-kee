{
  "name": "malcolm-kee-workspace",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "build": "pnpm run build:sites && pnpm run merge:sites",
    "build:sites": "pnpm --filter v2 run build",
    "merge:sites": "tsx scripts/merge-output.ts",
    "preview": "serve public",
    "v2": "pnpm --filter v2"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.0",
    "@types/node": "^14",
    "fs-extra": "^11.1.0",
    "serve": "^14.1.2",
    "tsx": "^3.12.2"
  }
}
