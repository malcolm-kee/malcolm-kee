import React from 'react';
import './landing-page-header.scss';

export const LandingPageHeader = ({ children }) => (
  <header className="workshop-landing-page-header">{children}</header>
);

export default LandingPageHeader;
