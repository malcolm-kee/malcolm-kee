import * as React from 'react';
import { RoundedLinkButton } from '../Button';

export const WorkshopLandingPageFooter = () => (
  <nav className="py-2 my-2 text-center">
    <RoundedLinkButton to="/workshops">All Workshops</RoundedLinkButton>
  </nav>
);
